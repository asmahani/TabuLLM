name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.13"]
        langchain_core_line:
          - label: "1.3"
            langchain_core: "1.3"
          - label: "1.4"
            langchain_core: "1.4"

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package with dev dependencies
        run: pip install -e .[dev]

      - name: Install LangChain core compatibility target
        run: >
          pip install
          "langchain-core==${{ matrix.langchain_core_line.langchain_core }}.*"

      - name: Run tests
        run: pytest tests/ --tb=short --cov=tabullm --cov-report=xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage.xml
          use_oidc: true
